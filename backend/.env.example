# Example environment variables for the Nest backend
# Copy this file to `.env` and fill in values before running the app
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=yourpassword
DB_NAME=pos_db
# Optional: Nest environment
NODE_ENV=development
# Other existing envs (if any) used by the app
# APP_PORT=3000
